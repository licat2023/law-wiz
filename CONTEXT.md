# 智法宝 领域术语表（Context）

本文件是「智法宝」项目**领域术语的唯一权威来源**。它只回答一件事：**这个词在本项目里指什么、不指什么。**

本文件**不写实现细节、不写需求、不写设计**——那些在 `docs/` 下的各份文档里。当术语在代码、文档、口头讨论中出现分歧时，以本文件为准。

> ⚠️ 本项目法律领域术语的法律含义由其法定出处决定，本表只裁定**本项目的用法**；与法定含义冲突时，以法律为准，并应回头修正本表。

---

## 一、平台与角色

**智法宝（law-wiz）**：
本平台的名称。面向中小企业与个人用户的一站式智能化法律服务平台。
_避免_：法律助手、法务通、LawWiz（除非指代码仓库名）

**用户**：
在本平台注册并拥有账户的自然人或企业主体。
_避免_：账号（账号是登录凭据，用户是主体）

**企业主体**：
以企业身份注册、可持有企业印章的用户。一个企业主体下可有多个自然人成员。

**签署方**：
被发起人指定为某份合同签署参与者的主体（自然人用户或企业主体）。一份合同可以有多个签署方。
_避免_：签署人（"签署人"指实际执行签署动作的自然人，与"签署方"是主体与执行者的区别）

**发起人**：
创建签署任务、指定签署方与签署顺序的用户。

---

## 二、身份与信任

**实名认证**：
核验用户真实身份的过程。个人认证与企业认证是两条不同的路径，不可互相替代。
_避免_：认证（过于宽泛）、身份验证（指登录时的凭据校验，见下）

**身份验证**：
登录时校验用户凭据（密码、验证码）的过程。**与实名认证不是一回事**：身份验证回答"你是不是这个账号的主人"，实名认证回答"这个账号背后是谁"。
_避免_：认证、鉴权（鉴权指权限判定）

**鉴权**：
判定已登录用户是否有权执行某操作的过程。
_避免_：授权（授权是把权限**授予**某人，见下）、认证

**授权**：
把某项权限**授予**某个用户或角色的动作。产生的结果叫**授权记录**。
_避免_：鉴权、认证

**可信时间戳**：
由**独立于本平台的第三方时间源**签发、可被第三方验证的时间戳凭证。其"可信"来自**信任锚不在本平台**。
_避免_：时间戳（见下）、上链时间

**时间戳**：
记录某一时刻的时间标记。**本平台自行生成的时间戳不构成可信时间戳**——它只能证明"本平台在此时记录了它"，不能向第三方证明该时刻的真实性。

**存证编号**：
本平台为每一次存证生成的业务标识，用于用户检索与出示。**不等于**区块链交易哈希。

**区块链交易哈希（TxID）**：
存证数据上链后由区块链网络返回的交易标识，可用于在链上独立验证该次存证。
_避免_：存证编号（两者不可互换使用）

**存证证书**：
本平台出具的、包含文件哈希值、可信时间戳、TxID、存证时间与存证编号的电子凭证（PDF）。它是**本平台对已发生事实的陈述**，不是第三方公证文书。

---

## 三、合同与签署

**合同**：
本平台中经起草或上传、处于某一生命周期状态的协议文档实体。生命周期状态：`草稿` → `待签署` → `签署中` → `已签署` → `已归档`。

**合同模板**：
预设条款结构与可填字段的合同骨架，用于快速起草。模板可含预设签署位置与填写字段。

**合同参与方**：
与某份合同存在权利或义务关系的主体。**范围大于"签署方"**：见证方、审批方也是参与方但不是签署方。
_避免_：签署方（是参与方的子集）

**签署任务**：
围绕一份合同组织一次多方签署的流程实例，包含签署方、签署顺序、各方的签署状态与截止时间。

**签署模式**：
签署任务中各方签署的先后约束。本平台支持三种：**单人单签**、**多人顺序签**（按指定顺序依次签）、**多人并行签**（无顺序要求，全部完成即结束）。

**签署意愿确认**：
签署人在签署前明确表达"我同意签署本合同"的动作。它是**可靠电子签名的构成要件之一**，不是可有可无的界面步骤。

**签署位置**：
合同文档中为签名或印章预留的坐标区域。

---

## 四、电子签名与印章（本组术语的法律含义互不相同，务必区分）

**电子签名**：
《电子签名法》定义：数据电文中以电子形式所含、所附，用于识别签名人身份并表明签名人认可其中内容的数据。**它是一种"数据"，不是图片，也不是流程**。
_避免_：数字签名（是电子签名的一种实现技术，见下）、电子签章（是动作，见下）

**可靠电子签名**：
《电子签名法》第十三条规定的、同时满足四项条件的电子签名（制作数据专属于签名人、签署时由签名人控制、签署后对签名的任何改动可被发现、签署后对数据电文内容和形式的任何改动可被发现）。**只有可靠电子签名才与手写签名或盖章具有同等法律效力。**
_避免_：电子签名（不是所有电子签名都可靠，两者不可混用）

**数字签名**：
基于非对称密码技术对数据计算出的密码学值，用于验证数据完整性与签名者身份。**它是实现电子签名的一种技术手段**，不等同于电子签名，更不等同于可靠电子签名。

**电子印章**：
基于可靠密码技术制作的、与实物印章具有同等法律效力的电子签章形式。印章包含印章图像数据、印章名称、所有者信息与电子签名认证证书。

**电子签章**：
**动作**，指在电子文档上加盖电子印章或添加电子签名的操作过程。
_避免_：电子印章（是物，不是动作）、电子签名（是数据，不是动作）

**用印**：
在本平台语境下特指**企业电子印章的使用行为**，是电子签章的一种。用印需经授权或审批。
_避免_：签章（过于宽泛，个人签名也应算签章）

**印章类型**：
公章、合同专用章、财务专用章、部门章、法人名章等。

**签名图片**：
用户手写绘制或上传的、用于视觉呈现的位图。**它不是电子签名**——签名图片可被复制粘贴，不满足"制作数据专属于签名人"的条件。本平台允许使用签名图片，但**在对外表述与文档中不得称其为电子签名**。

**签章服务**：
提供电子签名与电子印章能力的服务方。本平台**自身不签发证书**，可靠电子签名能力由具备资质的第三方签章服务提供，本平台负责编排、定位与存证。

---

## 五、证据与存证

**证据材料**：
用户上传的、拟用于证明某项事实的文件（合同、签名、照片、录音、视频等）。

**存证**：
将证据材料的哈希值与元数据记录到区块链上的行为。存证**固定的是"某份文件在某时刻已存在且此后未变"**，它不判断该文件的内容是否真实、是否合法。
_避免_：公证（公证由公证机构作出，本平台不提供公证）

**哈希值**：
对文件二进制数据计算得到的固定长度摘要，用于校验数据完整性。本平台统一使用 SHA-256（见 ADR-0004）。

**验签**：
验证一份已签署文档上的电子签名是否有效、文档内容签署后是否被篡改的过程。**验签只回答"有效/无效"与"是否被篡改"，不回答"签署人当时是否知情"。**

**指纹（文件指纹）**：
本项目中指文件哈希值的通俗说法。**与生物特征指纹无关**。

---

## 六、人工智能能力

**合同智能审查**：
对合同文本进行法律与商务风险识别的过程。产出**风险审查报告**。

**风险点**：
审查发现的、需要用户注意的具体问题，含**风险等级**（高/中/低）、**所在条款**、**问题描述**、**法律依据**与**修改建议**。

**风险等级**：
风险点的严重程度分级（高/中/低）。它是**本平台依据规则与模型给出的判断**，不是法律意见，也不构成对合同效力的评价。

**合同风险审查报告**：
合同智能审查的产出物（PDF/HTML），由摘要、风险点列表、法律依据与修改建议构成。
_避免_：法律意见书（本平台不出具法律意见书）

**法律知识库**：
用于检索增强生成（RAG）的法律语料集合。其**分级**为：核心法规（P1）、高价值案例（P2）、全量案例（P3，本期不建）。分级的裁定见《03-概要设计说明书》。

**知识库文档**：
法律知识库中一篇可独立检索与引用的语料单元（如一条法条、一份司法解释、一份合同范本、一份裁判文书）。
_避免_：文件（"文件"指用户上传的材料，与知识库语料不是一回事）

**法条引用**：
法律问答或合同审查结论中给出的、**具体到条**的法律依据（如《民法典》第五百七十七条），而非笼统提及某部法律。

**回答溯源**：
法律问答的产出中，标注回答所依据的知识库文档与法条引用，使用户可自行核对。

**AI 幻觉**：
大语言模型生成看似合理但无依据的内容。本项目通过检索增强生成与回答溯源**降低**该风险，**不宣称消除**。

**风险规则**：
人工编写的、以「条件—结论—法条依据」形式表达的结构化审查规则。它在检索效果不佳时作为兜底判断依据，见《03-概要设计说明书》。

**条款提取**：
从合同全文中抽取结构化关键信息（合同主体、标的、金额、违约责任、保密义务、管辖法院、付款条款、合同期限等）的过程。

---

## 七、交付与分期

**一期（P1）**：
本期实现范围：**M2 合同智能审查** + **M3 法律 AI 问答**。

**二期（P2）**：
工程化兜底：部署与运维、M4 区块链证据存证最小闭环。

**三期（P3）**：
合同线上化：M6 电子合同生成与签署、M7 电子印章与签名管理。

**四期（P4）**：
合规与管控增强：多方签署模式、到期提醒、印章分级授权、停用与销毁、权限配置、安全审计。

**五期（P5）**：
**微信小程序端**移植。

**六期（P6）**：
**安卓 App** 移植。

**客户端**：
访问本平台的软件形态。本项目有三个客户端：**Web**（主，P1）、**微信小程序**（P5）、**安卓 App**（P6）。**不做 iOS。**
_避免_：端（过于含糊）、平台（"平台"在本项目指智法宝本身）

**多端**：
本项目"同一套 REST 接口 + 多个独立客户端"的形态。三端**不共用代码库**，共用的是接口契约。
_避免_：跨端（"跨端"通常指用一套代码编多端，与本项目的做法相反）

**纵切面**：
一条端到端的业务链路，覆盖从数据、后端服务、AI 能力到前端界面的完整实现路径。本项目按纵切面而非技术分层组织团队分工。
_避免_：模块（模块是功能划分，纵切面是**实现与责任的划分**）

**把关人**：
某一模块或纵切面的技术裁决人。其职责是评审与裁决，**不独占实现**——任何成员都可以向该模块提交代码。

**契约冻结**：
接口契约（[`docs/05-接口设计.md`](docs/05-接口设计.md)，机器可读形式为 `/openapi.json`）在某一时点被确认为基线，此后变更须经知会与记录的过程。冻结不禁止变更，只要求**变更可见**。
